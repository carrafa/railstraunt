<%= form_for @table, url: user_tables_path(current_user) do |f| %>
<%= f.label :name %>
<%= f.select (:name) do %>
  <% [['A1'],['B1'], ['C1']].each do |c| %>
    <%= content_tag(:option, c.first, value: c.last) %>
    <% end %>
  <% end %>
<%= f.submit "add table"%>
<% end %>
