<%= form_for @party, url: user_parties_path(current_user) do |f| %>
<%= f.label "table" %>
<%= f.select (:name) do %>
  <% [['A'],['B'], ['C'], ['Bar']].each do |c| %>
    <%= content_tag(:option, c.first, value: c.last) %>
    <% end %>
  <% end %>
<%= f.submit "add party"%>
<% end %>
